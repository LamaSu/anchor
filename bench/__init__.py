"""anchor benchmark suite.

Run the full suite: `python -m bench.main`
Run a single sub-bench: `python -m bench.lifecycle`, etc.

Each sub-bench writes its slice of bench-results.json (under results/ by default)
so the HTML report and demo can assemble them into one dashboard.
"""
